console.log('OpenAI Vision is mocked for tests.');

module.exports = {
  analyzeImage: async () => {
    return { result: 'mocked result' };
  },
  // Weitere Mock-Methoden können hier ergänzt werden
}; 